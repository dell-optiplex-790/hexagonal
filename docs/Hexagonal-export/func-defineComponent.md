# Hexagonal.js Documentation
This file is for `hexagonal.defineComponent`. This will be moved into the `HexagonalApp` object for portability's sake. Example:
```typescript
import hexagonal from 'hexagonal.js';
const app = new hexagonal.App();
console.log(app);
```
Intended output: a `HexagonalApp` object