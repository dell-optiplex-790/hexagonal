import { App } from "./app";
import { state } from "./embeddedState";
import { Component, defineComponent, getComponents } from "./components";

export default {App, defineComponent, Component, getComponents, embeddedState: state}